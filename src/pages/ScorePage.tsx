import { Card, CardContent, Typography } from "@mui/material";

export const ScorePage = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">This is your Score</Typography>
      </CardContent>
    </Card>
  );
};
