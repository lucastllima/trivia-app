import { Card, CardContent, Typography } from "@mui/material";

export const HomePage = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Trivia App</Typography>
      </CardContent>
    </Card>
  );
};
